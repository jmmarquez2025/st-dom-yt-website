node_modules
dist
.vite
*.local
.env

# macOS Finder metadata
.DS_Store

# Local browser-audit screenshots (never ship in the repo)
.codex-*.png

# Claude Code worktree state — local-only scratch space
.claude/worktrees/

# Pre-optimization image backups (created by scripts/optimize-sources.mjs)
*.original-large.jpg
